import { atom } from "recoil";

export const dashboardWidgetState = atom({
  key: "dashboardWidgetState",
  default: [],
});

export const recentJobState = atom({
  key: "recentJobState",
  default: [],
});

