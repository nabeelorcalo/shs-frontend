import * as React from "react";
import "../style.scss";

const SVGComponent = (props: any) => {
  // const { name, className, description, imgSignature, txtSignature, fontFamily, fileURL } = props;

  return (
    <></>
  )
};
export default SVGComponent;
